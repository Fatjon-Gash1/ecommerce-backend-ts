<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Holiday Promotion</title>
        <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            background: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        .header {
            background-color: #ff6f61;
            color: #ffffff;
            text-align: center;
            padding: 15px;
            font-size: 24px;
            font-weight: bold;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
        }
        .content {
            padding: 20px;
            text-align: center;
            font-size: 16px;
            color: #333;
        }
        .loyalty-points {
            font-size: 22px;
            font-weight: bold;
            color: #ff6f61;
            margin: 10px 0;
        }
        .promotion-code {
            font-size: 18px;
            font-weight: bold;
            background: #ff6f61;
            color: #ffffff;
            padding: 10px;
            display: inline-block;
            margin: 10px 0;
            border-radius: 5px;
        }
        .cta-button {
            display: inline-block;
            background-color: #ff6f61;
            color: #ffffff;
            padding: 12px 20px;
            font-size: 18px;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
        }
        .footer {
            text-align: center;
            font-size: 14px;
            color: #777;
            padding: 15px;
            background: #f4f4f4;
            border-bottom-left-radius: 8px;
            border-bottom-right-radius: 8px;
        }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="header">Happy {{ holiday }}, {{ firstName }}!</div>
            <div class="content">
                <p>
                    We appreciate your loyalty! You have <span class="loyalty-points">{{ loyaltyPoints }} loyalty points</span> waiting for you.
                </p>
                {{#if promotionCode}}
                    <p>
                        As a special gift, enjoy <strong>{{ percentOff }}% off</strong> with this exclusive promo code:
                    </p>
                    <p class="promotion-code">{{ promotionCode }}</p>
                    <p>Click the button below to redeem your reward:</p>
                    <a href="{{ redeemUrl }}" class="cta-button">Redeem Now</a>
                {{/if}}
            </div>
            <div class="footer">
                <p>Thank you for being a valued member. Wishing you a joyful {{ holiday }}!</p>
            </div>
        </div>
    </body>
</html>
