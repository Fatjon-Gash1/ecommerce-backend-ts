<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Refund Request for order "{{ orderTrackingNumber }}"</title>
        <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 20px auto;
      background: #ffffff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
    }
    h2 {
      color: #333;
    }
    p {
      font-size: 16px;
      line-height: 1.5;
      color: #555;
    }
    .highlight {
      font-weight: bold;
      color: #007bff;
    }
    .footer {
      margin-top: 20px;
      font-size: 14px;
      text-align: center;
      color: #777;
    }
    .approved {
      color: #28a745;
    }
    .denied {
      color: #dc3545;
    }
        </style>
    </head>
    <body>
        <div class="container">
            {{#ifeq status "approved"}}
            <h2 class="approved">Refund Approved</h2>
            <p>
                We are pleased to inform you that your refund request for order <span class="highlight">#{{ orderTrackingNumber }}</span> has been <span class="approved"><strong>approved</strong></span>.
            </p>
            {{#if refundAmount}}
                <p>
                    You have received a refund of <span class="highlight">{{ refundAmount }}</span> out of <span class="highlight">{{ orderTotal }}</span> order total.
                </p>
            {{ else }}
                <p>
                    You have received a full refund of <span class="highlight">{{ orderTotal }}</span>.
                </p>
            {{/if}}
        {{ else }}
            <h2 class="denied">Refund Denied</h2>
            <p>
                We regret to inform you that your refund request for order <span class="highlight">#{{ orderTrackingNumber }}</span> has been <span class="denied"><strong>denied</strong></span>.
            </p>
            {{#if rejectionReason}}
                <p>
                    Reason for denial: <strong>{{ rejectionReason }}</strong>
                </p>
            {{/if}}
            <p>If you have any questions or need further clarification, please do not hesitate to contact our support team.</p>
            <p>Thank you for your understanding.</p>
        {{/ifeq}}
        <p>Best regards,</p>
        <p>
            <strong>Edge Tech</strong>
        </p>
        <div class="footer">&copy; 2025 Edge Tech. All rights reserved.</div>
    </div>
</body>
</html>
