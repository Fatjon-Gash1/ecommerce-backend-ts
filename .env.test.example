# General
NODE_ENV=test
BASE_PATH=/path/to/project # e.g., /app in Docker

# MySQL (test database)
MYSQL_USER=<test-user>
MYSQL_PASSWORD=<test-password>
MYSQL_DATABASE=<test-database>
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3307

# Redis (test database)
REDIS_HOST=127.0.0.1
REDIS_PORT=6380

# JWT / tokens
ACCESS_TOKEN_KEY=<access-token-key>
REFRESH_TOKEN_KEY=<refresh-token-key>
ACCESS_TOKEN_EXPIRY=7200000
REFRESH_TOKEN_EXPIRY=24h
GENERIC_TOKEN_KEY=<generic-token-key>

# Gmail (email sending)
GMAIL_USER=<test-email@example.com>
GMAIL_PASS=<test-password>

# LLM / API
LLM_API_KEY=<test-api-key>
LLM_NAME=<llm-name>
LLM_PROVIDER_API=<llm-provider-url>

# Stripe
STRIPE_KEY=<stripe-test-key>

# Client
CLIENT_URL=http://localhost:3001

# Application-specific settings
REGISTRATION_LOYALTY_POINTS=200

